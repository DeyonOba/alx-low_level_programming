#include "main.h"
#include <string.h>

/**
 * _strcat - Concatenate two strings
 *
 * @dest: Takes in a string character
 * @src: Takes in a string character
 * Return: A concatenated string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}

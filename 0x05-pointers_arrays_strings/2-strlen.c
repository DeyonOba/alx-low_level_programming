#include "main.h"
#include <string.h>

/**
 * _strlen - a function that returns the length of a string.
 *
 * @s: String characters
 * Return: Length of string
 *
 */
int _strlen(char *s)
{
	return (strlen(s));
}

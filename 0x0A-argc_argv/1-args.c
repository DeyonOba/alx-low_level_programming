#include <stdio.h>

/**
 * main - Print number of argument pass in the command line
 *
 * @argc: Argument count
 * Return: 0 (Success), -1 (Error)
 */
int main(int argc)
{
	printf("%d\n", argc);
	return (0);
}


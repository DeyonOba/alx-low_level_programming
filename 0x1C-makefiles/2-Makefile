# compiler to be used
CC = gcc
# source code file
SRC = $(wildcard *.c)
# object file
OBJ = ($SRC:.c=.o)
# excuteable name
Name = school

# default rule
all: $(Name)

# rule to build the executable
$(Name) : $(OBJ)
	$(CC) -o $@ $^

# rule to create object files
%.o : %.c
	$(CC) -c -o $@ $<


